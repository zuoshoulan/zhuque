# 朱雀项目渐进式开发策略

## 🎯 核心原则

**小步快跑，每步可验证，持续交付价值**

---

## 📋 渐进式开发路线图

### Milestone 0: 项目骨架搭建（1-2天）
**目标**: 让项目能跑起来，输出"Hello World"

#### 后端部分
```bash
✅ 步骤1: 创建v2目录和基础Maven结构
✅ 步骤2: 创建父POM，配置Spring Boot 3.2.x依赖
✅ 步骤3: 创建最小启动类
✅ 步骤4: 创建一个测试Controller输出"Hello World"
✅ 步骤5: 启动应用，访问 http://localhost:8080/actuator/health
```

**验证点**:
- [ ] Maven构建成功 (`mvn clean install`)
- [ ] 应用启动无报错
- [ ] 健康检查接口返回 `{"status":"UP"}`
- [ ] Hello接口返回 `{"message":"Hello World"}`

---

### Milestone 1: 认证系统 - 最小可用版本（3-5天）
**目标**: 能登录，能鉴权

#### 1.1 数据库准备
```bash
✅ 步骤1: 只创建用户表（user）
   - id, username, password, create_time

✅ 步骤2: 初始化一个测试用户
   - username: admin
   - password: admin123 (BCrypt加密)
```

**验证点**:
- [ ] MySQL中user表存在
- [ ] 可以查询到测试用户

#### 1.2 后端认证接口
```bash
✅ 步骤1: 实现登录接口 POST /api/auth/login
   请求: {"username":"admin","password":"admin123"}
   响应: {
     "code": 200,
     "data": {
       "token": "eyJhbGciOiJIUzI1NiIs...",
       "userInfo": {"id":1,"username":"admin"}
     }
   }

✅ 步骤2: 实现Token验证
   使用Postman测试，请求头带上token能访问受保护接口
```

**验证点**:
- [ ] Postman登录成功返回token
- [ ] 使用token访问 /api/auth/user-info 返回用户信息
- [ ] 不带token访问受保护接口返回401

#### 1.3 前端登录页面
```bash
✅ 步骤1: 创建Vue 3项目基础结构
✅ 步骤2: 只做登录页面（一个表单）
✅ 步骤3: 调用登录接口，存储token
✅ 步骤4: 登录成功跳转到简单的首页
```

**验证点**:
- [ ] 打开浏览器看到登录页面
- [ ] 输入admin/admin123点击登录
- [ ] 登录成功跳转到首页
- [ ] 首页显示"欢迎，admin"

**交付物**: 一个可以登录的前端页面 + 认证接口

---

### Milestone 2: 用户管理 - 完整CRUD（2-3天）
**目标**: 实现用户的增删改查，验证前后端打通

#### 2.1 后端接口
```bash
✅ 步骤1: 用户列表接口 GET /api/users
   - 支持分页
   - 响应: {
       "code": 200,
       "data": {
         "records": [...],
         "total": 10
       }
     }

✅ 步骤2: 创建用户接口 POST /api/users
✅ 步骤3: 更新用户接口 PUT /api/users/{id}
✅ 步骤4: 删除用户接口 DELETE /api/users/{id}
```

**验证点**:
- [ ] Postman测试所有接口成功
- [ ] 分页查询正常
- [ ] 创建后能查询到
- [ ] 更新后数据变化
- [ ] 删除后查不到

#### 2.2 前端页面
```bash
✅ 步骤1: 用户列表页面
   - 表格展示用户
   - 分页组件

✅ 步骤2: 新增用户对话框
✅ 步骤3: 编辑用户对话框
✅ 步骤4: 删除确认
```

**验证点**:
- [ ] 页面显示用户列表
- [ ] 点击新增弹出对话框，填写后提交成功
- [ ] 点击编辑弹出对话框，修改后提交成功
- [ ] 点击删除确认后，列表中该用户消失
- [ ] 分页切换正常

**交付物**: 完整的用户管理功能（前后端打通）

---

### Milestone 3: 广告主管理 - 业务模块验证（3-4天）
**目标**: 实现一个真实业务模块，验证分层架构

#### 3.1 数据层
```bash
✅ 步骤1: 创建广告主表 (advertiser)
✅ 步骤2: 使用MyBatis-Plus Generator生成Mapper和Entity
```

**验证点**:
- [ ] Mapper接口可以注入使用
- [ ] 可以执行简单的增删改查

#### 3.3 服务层
```bash
✅ 步骤1: 定义AdvertiserService接口
✅ 步骤2: 实现AdvertiserServiceImpl
✅ 步骤3: 添加业务逻辑（如：状态校验）
```

**验证点**:
- [ ] Service层单元测试通过
- [ ] 业务逻辑正确执行

#### 3.4 接口层
```bash
✅ 步骤1: 创建AdvertiserController
✅ 步骤2: 实现CRUD接口
```

**验证点**:
- [ ] Postman测试所有接口
- [ ] Swagger文档正确生成

#### 3.5 前端页面
```bash
✅ 步骤1: 广告主列表页
✅ 步骤2: 广告主表单（含更多字段）
```

**验证点**:
- [ ] 完整的广告主CRUD流程
- [ ] 表单验证正常
- [ ] 数据展示正确

**交付物**: 广告主管理功能

---

### Milestone 4: 推广活动模块（2-3天）
**目标**: 复用Milestone 3的模式，快速开发

#### 简化流程
```bash
✅ 步骤1: 创建campaign表
✅ 步骤2: 复制广告主模块的代码结构
✅ 步骤3: 修改字段和业务逻辑
✅ 步骤4: 前端页面开发
```

**验证点**:
- [ ] 完整的活动CRUD
- [ ] 与广告主关联正确

**交付物**: 推广活动管理功能

---

### Milestone 5: RTB竞价核心（5-7天）
**目标**: 实现竞价请求处理

#### 5.1 协议定义
```bash
✅ 步骤1: 定义简化版BidRequest
   - 不直接用Protobuf，先用JSON
   - 只包含核心字段

✅ 步骤2: 定义BidResponse
```

**验证点**:
- [ ] JSON结构定义清晰

#### 5.2 竞价接口
```bash
✅ 步骤1: 创建RTB接口 POST /rtb/bid
✅ 步骤2: 硬编码返回一个固定创意
```

**验证点**:
- [ ] Postman发送竞价请求
- [ ] 返回正确的BidResponse

#### 5.3 匹配逻辑
```bash
✅ 步骤1: 从数据库查询可投放的广告组
✅ 步骤2: 简单的出价逻辑（固定价格）
✅ 步骤3: 返回匹配的创意
```

**验证点**:
- [ ] 不同的请求能返回不同的广告
- [ ] 日志打印匹配过程

**交付物**: 可用的RTB竞价接口

---

### Milestone 6: 监测服务（2-3天）
**目标**: 实现曝光和点击监测

#### 6.1 曝光监测
```bash
✅ 步骤1: 创建监测表 tracking_log
✅ 步骤2: 实现曝光接口 GET /pixel/imp
✅ 步骤3: 异步写入数据库
```

**验证点**:
- [ ] 访问监测URL返回1x1 GIF
- [ ] 数据库中有记录
- [ ] 响应时间 < 100ms

#### 6.2 点击监测
```bash
✅ 步骤1: 实现点击接口 GET /pixel/click
✅ 替光302重定向到落地页
```

**验证点**:
- [ ] 访问点击URL正确重定向
- [ ] 数据库记录点击

**交付物**: 可用的监测服务

---

### Milestone 7: 完善和优化（持续进行）
**目标**: 补全其他模块，优化性能

- 广告组管理
- 创意管理
- 素材上传
- 数据统计
- 性能优化
- 单元测试

---

## 🎯 每个Milestone的验证清单

### 后端验证
```bash
1. 代码编译通过
   mvn clean compile

2. 单元测试通过
   mvn test

3. 应用启动成功
   mvn spring-boot:run

4. 接口测试通过
   - 使用Postman/Thunder Client
   - 或者写集成测试

5. Swagger文档可访问
   http://localhost:8080/swagger-ui.html
```

### 前端验证
```bash
1. 代码编译通过
   npm run build

2. 开发服务器启动
   npm run dev

3. 页面功能测试
   - 手动操作
   - 或使用Playwright/Cypress

4. 接口调用成功
   - 打开浏览器DevTools
   - 查看Network请求
```

### 联调验证
```bash
1. 前端能正常调用后端接口
2. 数据正确展示
3. 错误处理正确
4. 用户体验良好
```

---

## 📅 迭代周期建议

### 每个Milestone: 2-7天
- 小的Milestone（用户管理）: 2-3天
- 中的Milestone（广告主管理）: 3-4天
- 大的Milestone（RTB竞价）: 5-7天

### 每日工作流
```
早上:
- 查看Todo List，确定今天要完成的步骤
- 预估每个步骤的时间

开发中:
- 每完成一个功能点，立即验证
- 不写完一个Milestone不合并到主分支

晚上:
- 提交代码到Git
- 更新PROJECT_STATUS.md
- 记录遇到的问题和解决方案
```

---

## 🔄 持续集成检查点

### 每次提交前检查
```bash
✅ 代码编译通过
✅ 本地测试通过
✅ 没有新增TODO
✅ Git提交信息清晰
```

### 每个Milestone完成后
```bash
✅ 所有验证点通过
✅ 更新PROJECT_STATUS.md
✅ Git提交并打tag（可选）
✅ 简短记录本次迭代内容
```

---

## 💡 降级方案

如果某个Milestone遇到困难，可以：

### 方案1: 缩小范围
- RTB竞价太复杂 → 先做简化版，只返回固定广告
- 前端页面太复杂 → 先只做列表，不做表单

### 方案2: 分拆步骤
- Milestone太大 → 拆成2-3个小Milestone
- 每个小Milestone独立可验证

### 方案3: 暂时跳过
- 非核心功能可以延后
- 先保证主流程可用

---

## 📊 进度可视化

### 使用Git分支
```bash
main                # 稳定版本
develop             # 开发主分支
feature/milestone-0 # 各个功能分支
feature/milestone-1
feature/milestone-2
...
```

### 使用Git Tag
```bash
v0.1.0-m0   # Milestone 0 完成
v0.1.0-m1   # Milestone 1 完成
v0.1.0-m2   # Milestone 2 完成
...
```

---

## ✅ 成功标准

### 每个Milestone必须满足
1. **可运行**: 代码能跑起来
2. **可测试**: 有明确的验证点
3. **可演示**: 能给他人展示功能
4. **可回滚**: Git提交清晰，有问题可以回退

### 最终交付标准
1. 核心业务流程完整
2. 前后端联调通过
3. 主要功能有测试覆盖
4. 文档齐全

---

## 🎓 最佳实践

1. **先跑通，再优化**
   - 第一版不求完美，但求能跑

2. **频繁提交，小步前进**
   - 每完成一个小功能就提交
   - 不要积累大量代码

3. **立即验证，发现问题及时改**
   - 不要等写完一起测
   - 边写边测

4. **保持简单**
   - 能用简单方案就不用复杂的
   - 不过度设计

5. **记录问题**
   - 遇到的坑记录下来
   - 避免重复踩坑

---

**文档版本**: v1.0
**创建日期**: 2025-01-05
**适用项目**: 朱雀广告平台v2
